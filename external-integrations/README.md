# External Integration

Placeholders for external integration adapters (APIs, webhooks, connectors).

Add service adapters here, for example:

- `example_integration.py` — sample webhook client
